% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_ffsimulator.R
\name{simulate_player_week_outcomes}
\alias{simulate_player_week_outcomes}
\title{Generate one-week player outcome draws with ffsimulator}
\usage{
simulate_player_week_outcomes(
  rankings,
  adp_outcomes,
  n_simulations = 1000L,
  week = 1L,
  seed = NULL,
  source = NULL,
  rosters = NULL,
  strict = TRUE
)
}
\arguments{
\item{rankings}{A normalized ranking table, or a provider data frame.}

\item{adp_outcomes}{A weekly outcome pool from \code{ffsimulator}.}

\item{n_simulations}{Number of weekly simulations.}

\item{week}{Week number to attach to every returned draw.}

\item{seed}{Optional random seed.}

\item{source}{Source name when \code{rankings} is not normalized.}

\item{rosters}{Optional roster table with \code{player_id} or \code{fantasypros_id}.}

\item{strict}{If \code{TRUE}, error when the historical pool does not support all
sampled rank draws. If \code{FALSE}, fill unsupported rows with zero points.}
}
\value{
A data frame with one row per player and simulation.
}
\description{
This uses \code{ffsimulator::ffs_generate_projections_week()} and returns one
weekly score per player and simulation. The historical pool must contain
\code{pos}, \code{rank}, and nested numeric \code{week_outcomes} columns. A season pool
from \code{ffsimulator::ffs_adp_outcomes()} is also accepted because it has the
same rank-conditioned score columns.
}
